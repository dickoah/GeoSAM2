# Runtime dependencies for GeoSAM2.
# These are also declared in pyproject.toml; the file is kept for users who
# prefer `pip install -r requirements.txt` over installing the package itself.
numpy
torch>=2.3
torchvision
iopath
pillow
opencv-python<5
tqdm
trimesh
ninja

# ── Step-by-step app (app.py) ────────────────────────────────────────────
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9   # required by FastAPI for the multipart upload endpoints

# Downloads ckpt/geosam2.pt from Hugging Face when it is missing (utils/segmenter.py).
huggingface_hub

# pyrender renders the 12 views (util/views.py) but pins PyOpenGL==3.1.0, which
# cannot texture a mesh under numpy 2. Newer PyOpenGL here, pyrender separately:
#   pip install --no-deps 'pyrender>=0.1.45'
PyOpenGL>=3.1.7
freetype-py
imageio
pyglet>=1.4.10
six

# VLM describe/paint of the seed view (utils/guidance.py, via SegviGen's code).
# >=2.11 moved ImageGenerationTool and BinaryImage to the package root.
pydantic-ai>=2.11

# Loads GEMINI_API_KEY from .env. pydantic-ai's Google provider reads either
# GOOGLE_API_KEY or GEMINI_API_KEY, despite only naming the first when it fails.
python-dotenv>=1.0

# Connected components and distance transforms for the point prompts
# (utils/auto_prompt.py). Also pulled in by pyrender.
scipy>=1.10

