# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C / CUDA build artifacts
*.so
*.o
*.obj
build/
dist/
*.egg-info/
*.egg

# Local virtual environments
.env
.venv
env/
venv/
ENV/

# Editor / IDE, and the coding agents' own instructions: local-only, never
# pushed. AGENTS.md and CLAUDE.md live in .claude/ so agents still read them.
.idea/
.vscode/
.claude/
*.swp
*.swo
.DS_Store

# Project artifacts
ckpt/
checkpoints/
outputs/
exp/
logs/
wandb/
*.log

# Hydra
.hydra/
multirun/

# Jupyter
.ipynb_checkpoints/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/

# Comparison sheets written by `python -m utils.render --images`.
# Regenerate them; they are renders, not source.
example/render_comparison/

# Samples built from meshes that live elsewhere, and the results scored off
# them. Renders and label arrays, not source: `python -m utils.sample <mesh>
# example/sample_04` and `python -m app.multiview_demo` rebuild them. The
# bundled sample_00..03 stay tracked -- they are the reference data.
example/sample_0[4-9]/
example/sample_1*/
example/sample_*_our/
example/multiview_results/
